# Optional: override the model checkpoint path.
# Relative paths are resolved from the project root by the API.
MODEL_WEIGHTS=deepfake_model_best_v2.pth

# Maximum uploaded video size in MB.
MAX_UPLOAD_MB=100

# Comma-separated frontend origins. Use * for local development.
CORS_ORIGINS=*
